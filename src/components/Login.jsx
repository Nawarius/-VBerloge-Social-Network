import React from 'react';

const Login = (props)=>{
	debugger;
	return <div>
		<div>Login: {props.login}</div>
		<div>Email: {props.email}</div>
		<div>ID: {props.id}</div>
	</div>
}

export default Login